#!/usr/bin/env python3
import os
import sys

_here = os.path.dirname(os.path.realpath(__file__))
_src = os.path.join(os.path.dirname(_here), "src")
if os.path.isdir(_src) and _src not in sys.path:
    sys.path.insert(0, _src)

from nocwall.cli import main

sys.exit(main())
